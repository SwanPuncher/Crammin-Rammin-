using UnityEngine;
using System.Collections;

public class TouchNoodle : MonoBehaviour {

	// Use this for initialization
	void Start () {
		//trying to remember a solution to have one script that detects touch active and will check objects tapped
		//This is instead of having an OnTouch() on every object which will undoubtedly be expensive. 
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
